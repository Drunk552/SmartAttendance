# Build-time configuration

This directory owns checked-in feature defaults and bounded resource limits.
Runtime databases, models, captures and exports remain under the selected
runtime directory and must not be committed. Board-specific device nodes and
GPIO values are intentionally absent until the board wiring and BSP are fixed.
