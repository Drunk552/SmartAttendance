# SmartAttendance 仓库开发约束

本文件适用于整个仓库，供 Codex、其他 AI 工具和项目开发者执行任务时使用。详细架构与工程要求以以下文档为准：

- `docs/markdowm/嵌入式项目重构目录方案.md`
- `docs/嵌入式工程化开发规范.md`

如果本文件、任务要求和现有实现发生冲突，先说明冲突和影响，不得静默改变架构方向或硬件假设。

## 1. 项目定位与当前阶段

- 本项目是嵌入式 Linux 离线人脸考勤终端，不按 Web 前后端项目设计。
- 当前先保证 PC/WSL 仿真流程可构建、可运行、可测试，再适配树莓派 Zero 2 W 原型，最后评估 64/128MB 低成本 Linux 平台。
- 当前仓库只包含软件原型，不包含完整 BSP、量产板 SDK、交叉编译工具链、系统镜像和烧录流程。
- 重构必须小步进行；一次任务只处理一个明确模块或调用链，并保留可编译、可测试、可回退的状态。

## 2. 已冻结的技术基线

| 技术项   | 约束                                       |
| -------- | ------------------------------------------ |
| C++      | C++17，禁止使用 C++20/23 语言和标准库能力  |
| C        | C11                                        |
| LVGL     | 9.4.0，以`libs/lvgl/lv_version.h` 为准   |
| CMake    | 最低 3.16                                  |
| 当前主机 | Ubuntu 20.04/22.04 或带图形支持的 WSL/WSLg |

禁止引入 `std::expected`、`std::jthread`、`std::stop_token`、`std::span`、`std::format` 和 `std::ranges`。需要相似能力时使用符合 C++17 的项目实现。

OpenCV、SQLite、SDL2、Freetype、libxlsxwriter、摄像头管线、BSP 和目标工具链的精确版本当前未冻结。目标硬件确定后再记录版本、来源、许可证、编译选项、ABI 和校验值，不得把 PC 环境中的版本直接声明为量产基线。

## 3. 主机环境与工具链

### 3.1 当前可用主机构建

当前主机依赖和安装方式见 `README.md`。构建至少需要：

```text
CMake 3.16+
支持 C++17 的 GCC/Clang
pkg-config
SDL2
Freetype
OpenCV 4（当前 PC 完整版本包含 face 等模块）
SQLite3
libxlsxwriter
POSIX Threads
```

不得在源码或 CMake 中新增宿主机专属绝对路径。现有 `/usr/include/opencv4` 属于待重构遗留项，不得复制到新 Target 或平台实现中。

### 3.2 交叉编译工具链

交叉编译工具链尚未配置。建立工具链时必须：

- 使用独立的 `CMAKE_TOOLCHAIN_FILE` 和目标 sysroot。
- 记录编译器、链接器、目标三元组、CPU/ABI、C 库和 sysroot 来源。
- 禁止链接宿主机 `/usr/lib`、`/usr/local/lib` 或宿主机头文件。
- 每个目标平台使用独立构建目录，不得在 PC 构建目录中切换平台变量继续增量编译。
- 工具链未落地前，不得声称已经完成板端交叉编译验证。

计划构建配置为 `simulator-full`、`rpi-zero2w` 和 `target-lowmem`。当前尚无 `CMakePresets.json`，不得把这些名称当成已经可执行的 Preset 命令。

## 4. 当前构建、运行和清理命令

所有命令默认从仓库根目录执行。

### 4.1 配置与编译

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build --parallel
```

不得依赖个人 Shell 中由 `env/env.sh` 定义的 `make` 函数作为唯一构建方式；自动化验证使用上面的标准 CMake 命令。

### 4.2 运行 PC 仿真程序

```bash
./build/attendance_app
```

摄像头预览和识别依赖当前 Windows FFmpeg 到 WSL UDP 5004 的推流环境。没有推流时只能验证不依赖摄像头的启动和 UI 行为，必须在结果中注明限制。

### 4.3 清理

优先执行目标级清理：

```bash
cmake --build build --target clean
```

确需重新配置时才删除明确的构建目录：

```bash
cmake -E remove_directory build
```

禁止对仓库根目录、用户目录或未解析变量执行递归删除。删除构建目录前确认其中没有用户需要保留的测试报告。

## 5. 测试与质量检查命令

### 5.1 单元测试

提交前至少执行：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build --parallel
ctest --test-dir build --output-on-failure
```

当前 CTest 只包含已接入的测试，测试数量少不代表未覆盖行为已经验证。修改业务规则、Presenter、队列、TaskManager 或 HAL 时必须同步补充相应测试。

### 5.2 运行冒烟与稳定性测试

修改启动、线程、UI、摄像头或资源生命周期时，在具备图形环境和运行依赖的主机上执行：

```bash
bash tools/verify_test.sh
```

10 分钟和 1 小时稳定性测试属于阶段验收，不要求每次小提交执行。相关脚本和 CMake Target 当前存在工作目录差异，修复并验证前不得把未实际运行的结果写成通过。

### 5.3 静态检查

仓库当前没有 `.clang-format`、`.clang-tidy` 或正式 cppcheck 配置，因此暂时没有统一的强制静态检查命令。新增配置前：

- 编译不得新增警告。
- 修改 Shell 脚本时执行 `bash -n <脚本路径>`。
- 修改 Python 工具时执行 `python3 -m py_compile <文件路径>`。
- 不得使用个人默认 clang-format 对整个仓库批量格式化。
- 不得声称 clang-tidy、cppcheck、ASan、UBSan 或 TSan 已通过，除非本次确实配置并执行。

后续引入静态检查必须同时提交配置文件、标准命令和基线处理规则。

## 6. 打包、部署和烧录

- 当前没有正式打包命令、部署脚本、升级包格式或烧录命令。
- 未获得具体板卡、存储介质、启动方式和厂商工具信息前，不得编造或执行 `dd`、厂商烧录器、分区写入和系统镜像命令。
- 树莓派原型通常属于系统镜像准备和应用部署，不等同于 MCU 固件烧录；具体流程确定后再写入本文件。
- 未来打包必须包含应用、配置、资源、数据库迁移、依赖清单、版本信息和校验值。
- 烧录或写盘属于破坏性操作，必须先确认设备节点和目标介质，并得到用户明确授权。

## 7. 目标板和硬件事实

当前仅确定：

- 实体原型平台：Raspberry Pi Zero 2 W。
- 后续目标：64/128MB 低成本嵌入式 Linux 平台，具体 SoC 和板卡未确定。
- Zero 2 W 是原型验证平台，不代表最终量产硬件和内存目标。

以下内容尚未冻结，不得自行假设：

```text
目标系统使用 32 位 ARM 还是 AArch64
目标 C 库使用 glibc 还是 musl
Linux 内核和 BSP 版本
交叉编译器与 sysroot
显示后端使用 framebuffer 还是 DRM
摄像头型号、像素格式和驱动
GPIO 编号、RTC、蜂鸣器、继电器和补光灯接线
系统镜像、分区和启动介质
```

硬件信息确定后，必须记录板卡修订版、CPU 架构、ABI、C 库、内核/BSP、设备树、驱动、设备节点和外设连接表。

## 8. 架构和依赖边界

- `Application` 是唯一组合根，只负责对象装配和生命周期顺序。
- `core` 只保存纯业务模型和规则，禁止依赖 LVGL、SQLite、OpenCV、SDL2、线程或具体硬件。
- `services` 编排业务流程，不直接创建数据库、摄像头和平台设备。
- `ui` 使用轻量 Presenter；Presenter 不持有 LVGL 控件、Repository、平台实现或长期线程。
- `hal` 定义最小硬件接口，`platform` 提供 PC、树莓派和低内存平台实现。
- 业务代码和 UI 代码禁止出现 GPIO 编号、设备节点和厂商 SDK 调用。
- 平台选择由 CMake 和 `PlatformFactory` 完成，禁止在业务流程中扩散 `#ifdef`。
- 后台线程到 UI 使用有界 `UiMessageQueue`；不新增全局通用 EventBus，不使用 `void*` 消息负载。
- 页面生命周期和同步业务调用使用普通函数调用，不经过消息总线。

## 9. 线程、资源和低内存约束

- 禁止新增 `std::thread::detach()`。
- 每个线程必须有明确创建者、停止条件、唤醒方式和 `join()` 所有者。
- 停止顺序统一为：停止新输入、`requestStop()`、关闭或唤醒阻塞队列、`join()`、销毁 Service/HAL/存储资源。
- LVGL API 只能由 UI 线程调用。
- 队列、缓存、线程数、线程栈、图像尺寸和重试次数必须有明确上限。
- 预览帧可以按策略丢弃旧帧；识别成功、打卡完成和设备故障不得静默丢弃。
- 禁止跨线程传递生命周期不明确的裸指针、局部对象地址或可变 `cv::Mat` 视图。
- 性能和内存优化必须提供修改前后的实测数据；没有真实板卡数据时标记为估算或未验证。

## 10. 不得修改或需特别保护的内容

- `libs/lvgl/` 是固定的 LVGL 9.4.0 第三方源码。除非任务明确要求升级、修补或裁剪 LVGL，否则不得修改。
- `docs/FA03H产品资料/` 保存原始产品 PDF，只读使用，不得覆盖、转换后替换或重命名原文件。
- 不得提交或修改生成目录中的构建产物，例如 `build/`、CMake 缓存、可执行文件和测试临时报告。
- `attendance.db`、`face_model.xml`、抓拍图片和导出文件属于运行时数据，不得作为源码修改或默认提交。
- 不得修改任务范围外的用户未提交改动；开始任务前检查 `git status --short`。
- 不得执行大范围文件移动、全仓格式化、第三方代码改写或无关清理。
- 不得删除数据库、模型、产品资料、用户资源或构建报告，除非用户明确授权并已确认准确目标。

可以修改根目录 `lv_conf.h`，但必须说明对 PC 和低内存构建的影响，并验证 UI 构建和运行行为。

## 11. 代码风格与注释

- 命名空间、文件名使用 `snake_case`。
- 类型和枚举使用 `PascalCase`。
- C++ 函数和局部变量使用 `lowerCamelCase`。
- 私有成员以 `_` 结尾，常量使用 `kPascalCase`，宏使用 `UPPER_SNAKE_CASE`。
- C 接口使用 `snake_case`。
- 标识符使用英文，注释和文档可以使用中文，所有源码使用 UTF-8。
- 头文件必须自包含；禁止在头文件中使用 `using namespace`；非模板实现放入 `.cpp`。
- 禁止裸 `new/delete` 表达资源所有权；优先使用 RAII 和 `std::unique_ptr`。
- 注释解释设计原因、线程、所有权、业务规则和硬件限制，不重复直译代码。
- 公共接口注释说明输入、输出、失败原因、线程、所有权、副作用和阻塞/超时行为。
- TODO 使用 `TODO(owner/issue-id): 内容和移除条件`，禁止无责任人的“以后处理”。
- 修改实现时同步检查相邻注释，禁止保留与代码不一致的注释。

## 12. 错误处理规范

- 不会失败的纯函数直接返回值。
- “未查询到”但不是错误时使用 `std::optional<T>`。
- 可预期业务、存储和设备失败使用项目 C++17 `Result<T, ErrorEnum>` 或明确模块错误枚举。
- 禁止用一个全局整数错误码表达所有模块失败。
- 禁止用异常表达用户不存在、重复打卡等正常业务分支。
- 第三方异常必须在模块边界捕获并转换，异常不得跨越 C API、线程入口和硬件回调。
- 禁止吞掉异常、只打印后返回成功或静默返回默认值。
- UI 提示、内部错误码和日志必须能够对应。
- 错误路径与资源释放路径必须纳入测试。

## 13. 日志规范

日志等级统一为 `TRACE`、`DEBUG`、`INFO`、`WARN`、`ERROR`、`FATAL`。

- 禁止新增散落的 `printf` 或 `std::cout` 作为正式日志系统。
- 日志至少包含时间、线程、模块、操作和错误码；设备错误应附带底层错误信息。
- 正式版本日志必须支持滚动和总容量限制。
- 禁止记录密码、人脸模板、指纹模板、密钥和完整敏感个人数据。
- 正式版本可以关闭 TRACE/DEBUG，但不能完全关闭 ERROR/FATAL。
- 日志系统尚未统一前，新增代码应把输出限制在模块边界，并避免形成新的日志接口事实标准。

## 14. 修改与提交要求

- 修改前阅读相关源码、测试和设计文档，先确认调用方、线程、所有权和错误传播。
- 优先新增或调整接口与测试，再迁移实现；不要一次同时进行大规模移动、业务修改、数据库变更和 UI 重做。
- 不扩大现有 `UiController`、全局单例、EventBus 和 `RPI_BUILD` 的使用范围。
- 不使用 `GLOB_RECURSE` 扩大新模块接入；新 Target 和源文件优先显式列出。
- 不通过 `|| true`、关闭警告或删除失败测试伪造验证通过。
- 不自动提交、推送、创建标签或改写 Git 历史，除非用户明确要求。
- 完成后报告改动文件、执行的命令、实际结果、未验证项、风险和回退方式。

## 15. 提交前验证门禁

所有源码修改至少执行：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build --parallel
ctest --test-dir build --output-on-failure
```

根据变更范围追加：

| 变更类型                     | 追加验证                                               |
| ---------------------------- | ------------------------------------------------------ |
| Shell 脚本                   | `bash -n <脚本路径>`                                 |
| Python 工具                  | `python3 -m py_compile <文件路径>`                   |
| 启动、线程、UI、资源生命周期 | `bash tools/verify_test.sh`，要求具备可运行图形环境  |
| LVGL 配置或页面              | PC 仿真启动、页面进入退出、输入和刷新人工验证          |
| 数据库                       | 临时数据库上的 CRUD、迁移、事务回滚和断电/失败路径测试 |
| 平台/HAL                     | PC fake 测试；有目标板时再执行独立驱动冒烟和整机联调   |
| 性能/低内存                  | 记录 RSS/PSS、线程数、队列深度和改动前后数据           |

如果因为缺少显示、摄像头、硬件、工具链或依赖无法执行某项验证，必须明确写“未验证”及原因，不能用 PC 测试替代板端结论。

## 16. 完成定义

任务只有同时满足以下条件才算完成：

```text
实现符合任务范围
构建通过
相关自动化测试通过
无新增编译警告
错误路径和资源释放已处理
文档与注释同步更新
不破坏 PC 仿真构建
未验证项被明确记录
有可执行验收方法和安全回退方案
```
